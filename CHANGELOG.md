# CHANGELOG

## v0.6.0 — 从"能读 PDF"到"读完并且用起来"

这一版把 SoloPDF 和主流阅读器逐项对比后缺的东西补齐了，同时没有动
"高亮即笔记"这条主线——所有新标注一样落进 `.annotations.md`。

### 阅读

- **旋转**：整档或单页，随文档记住
- **版式**：单页 / 双页对开（可让封面单独成页）× 连续滚动 / 单屏翻页
- **裁边**：自动检测白边（采样若干页取最保守值）+ 四边手动微调；
  只影响显示，不改文件。移动端读双栏论文的关键
- **自动滚动**（PDF 与图书模式都有）与**阅读时屏幕常亮**
- **朗读**：系统语音，按句朗读、跟随高亮、自动翻页；引擎拒绝时会说明原因，
  而不是静默跑完整本书
- **书签**：⌘D 或工具栏 ☆，侧栏成组管理

### 标注

- 新类型：**下划线 / 删除线 / 波浪线 / 便签图钉 / 框选截图**
- 截图存进 `<文档名>.annotations.assets/`，伴生 MD 里用 `![](…)` 引用——
  SoloMD、GitHub、任何 Markdown 阅读器都直接显示图
- **导出带标准 PDF 注释的副本**：Preview / Acrobat / 福昕终于也能看到你的高亮
  （原文件永不修改）
- 侧栏支持按类型 / 颜色 / `#标签` / 关键词筛选，按页码或时间排序

### 文档工具（全部写新文件，源文件只读）

- 页面管理：旋转 / 删除 / 提取 / 重排
- 合并、拆分（按范围或每 N 页）
- PDF → 图片、图片 → PDF、压缩（重采样内嵌图片）
- 手写签名与日期戳，在页面预览上拖放定位
- 设置 / 移除打开密码（AES-256；移除需要知道密码，本程序不做破解）

### 找东西

- **书架**取代欢迎页：封面网格、阅读进度、收藏、标签、排序；
  桌面可添加文件夹扫描，手机上书架本身就是文件管理器
- **全库搜索**：先搜伴生批注（即时），再搜正文（按需建索引，可中断）
- **离线词典**：内置 CC-CEDICT 12 万词条（4MB，gzip 分片按需加载），
  中文按最长前缀匹配；macOS/iOS 可直接唤起系统词典
- **阅读统计**：时长、连续天数、每本进度——只存本机，可导出/清空

### 新格式

- **MOBI / AZW3（KF8）**：自研解析器，处理了 trailing-entry 与 KF8 边界
- **CBZ / CBR 漫画**：专用阅读器，单页/双页、日漫右起翻页、按需解码
- **DjVu**：pure-Rust 解码（djvu-rs，MIT——参考实现 DjVuLibre 是 GPL，
  上不了 App Store）

### 平台

- **Android**：新增工程、构建脚本与 CI；`content://` URI 通过 JNI
  读进应用自己的目录，open-with 与 `solopdf://` 深链都可用
- **iOS 修复**：文件选择器与"用 SoloPDF 打开"给的都是临时路径，
  重启即失效——现在一律导入到应用自己的 Library 目录，按内容哈希去重，
  阅读进度与批注跟着走
- 移动端与桌面端分别设计：手机上面板走底部 sheet、44pt 触控目标、
  快捷键提示自动隐藏；桌面保留键盘优先与多选拖拽

### 工程

- `solopdf-doc` 原生驱动 + CLI 新子命令（annotate / to-images / search /
  dict / doc …），MCP 新增 6 个工具（其中 4 个写门控）
- 修掉一个隐蔽坑：pdf.js 会用**它自己解析到的** `@napi-rs/canvas` 打
  Path2D 补丁，pnpm 下与本包依赖不是同一份原生模块，任何带字体的页面
  都会在 `paintChar` 崩掉——看起来像 PDF 损坏。CLI 与 MCP 现在都从
  pdf.js 的解析根取 canvas
- `scripts/check-i18n.mjs`：四语言键完整性 + 占位符一致性，进 CI
- 13 项 pdfops Rust 测试 + 3 项 DjVu 测试 + core 单测，全部进 CI

### 已知限制

- CBR 只在真实 RAR 归档上做了 API 级验证（本机没有 RAR 打包器可造样本）
- MOBI 的 HUFF/CDIC 压缩与 DRM 文件不支持，会明确提示而不是显示乱码
- Android release 包需要你自己的 keystore；Play Console 账号也需你注册
- **iOS 最低系统版本提到 15.0**：Xcode 27 已经拒绝构建 14.0 目标（"the range of
  supported deployment target versions is 15.0 to 27.0.x"），不是警告而是硬失败，
  所以这是被工具链推着走的，不是主动取舍
